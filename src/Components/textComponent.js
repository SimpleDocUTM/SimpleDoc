class textComponent extends React.Component {
	render() {
		return (
		)
	}
}

ReactDOM.render(<textComponent/>, document.getElementById('root'));
const myelment = <textComponent text="n/a" />

//takes in text ids as props
//displays the text in card format

